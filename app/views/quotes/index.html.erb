<div class="navbar">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="#">Project Quote</a>
      <ul class="nav">
        <li class="active">
          <a href="#">Home</a>
        </li>
        <li><a href="#">Link</a></li>
        <li><a href="#">Link</a></li>
      </ul>
    </div>
  </div>
</div>


<h1>Listing quotes</h1>

<table class="table table-striped table-condensed">
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Costperhour</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @quotes.each do |quote| %>
  <tr>
    <td><%= quote.name %></td>
    <td><%= quote.description %></td>
    <td><%= quote.costperhour %></td>
    <td><%= link_to 'Show', quote, :class => "btn btn-primary" %></td>
    <td><%= link_to 'Edit', edit_quote_path(quote), :class => "btn btn-primary" %></td>
    <td><%= link_to 'Destroy', quote, :class => "btn btn-danger", confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Quote', new_quote_path, :class => "btn btn-success" %>
